ferret.module('blueos.api.application', function (require, exports, module) {
    // var FormApplication = require('FormApplication');
    // var apps = {};

    // function run(options) {
    //     var app;

    //     if (options.name && apps[options.name] != null) {
    //         app = apps[options.name];
    //         app.active();
    //     } else {
    //         app = new FormApplication(options);
    //         apps[options.name] = app;
    //         app.run();
    //     }
    // }

    // function list(cb) {
    //     cb(apps);
    // }

    // module.exports = {
    //     run: run,
    //     list: list
    // };
});
